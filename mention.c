#include "assem.h"
#include "op.h"
/*
void add_mention(t_pars *pars)
{
    t_token *head;

    head = pars->token;

}

void make_mantion(t_pars *pars)
{
    t_token *head;

    head = pars->token;
    while(pars->token)
    {
        if(pars->token->type == MENTION) 
            add_mantion(pars);
    }
}*/